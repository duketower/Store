# Workspace Task Queue

## Current

- Maintain clear boundaries between `POS/`, `Websites/`, and `Bots/`
- Keep project-level docs aligned with the current folder structure
- Keep the repo-level SonarCloud runbook current without storing live tokens in git

## Next

- Add new websites under `Websites/<domain>/`
- Add new bots under `Bots/<bot-name>/`
- Keep per-project task queues updated instead of tracking implementation details here

## Done

- Reorganized the workspace into three top-level folders
- Added workspace-level documentation scaffold
- Added a single repo-level SonarCloud runbook and local token storage convention
