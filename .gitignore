.env
credentials.json
__pycache__/
*.pyc
*.pyo
.DS_Store
venv/
*.egg-info/

# Client Firebase credentials — each client has their own, never commit these
clients/*/.env

# Build outputs — regenerated by scripts, no need to track
deployments/

# Root script dependencies
node_modules/
