.env
credentials.json
__pycache__/
*.pyc
*.pyo
.DS_Store
venv/
*.egg-info/

# Client configs — contain Firebase credentials and store secrets, never commit
clients/*/.env
clients/*/client.config.json

# Build outputs — regenerated by scripts, no need to track
deployments/

# Root script dependencies
node_modules/
